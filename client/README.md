# CS411 React Flow Frontend

This frontend is a Vite React app that visualizes public GitHub repository folders with React Flow and shadcn/ui-style components.

## Run

```bash
npm install
npm run dev
```

By default the app calls `http://localhost:8000`. Set `VITE_API_URL` to use another backend.
